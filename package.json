{
  "private": true,
  "scripts": {
    "start": "live-server --port=8080 --host=localhost --open=./index.dev.html",
    "build": "htmlmin -o index.html index.dev.html"
  },
  "devDependencies": {
    "live-server": "1.2.0",
    "htmlmin": "0.0.7"
  }
}
